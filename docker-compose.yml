services:
  pterodactyl-ai-bot:
    build: .
    container_name: pterodactyl-ai-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
